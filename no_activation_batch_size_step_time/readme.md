# Usage
In `train_gpt2.sh` change `batch_size` parameter in `bash run_comparison.sh --batch_size 1` line at the end for different batch_sizes.

All these experiments were conducted on 2 h200 single node